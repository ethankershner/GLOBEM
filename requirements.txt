# GLOBEM PyTorch Fork - Requirements
# Python >= 3.10 (tested with 3.13)
#
# PyTorch with CUDA must be installed separately BEFORE these packages:
#   pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu128
#
# This is required for Blackwell GPUs (RTX 50-series, sm_120).
# For older GPUs, stable PyTorch releases with CUDA 12.1/12.4 may work:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124

# Core scientific stack
numpy>=1.26
pandas>=1.3
scipy>=1.10
scikit-learn>=1.0
statsmodels>=0.13
joblib>=1.1

# Visualization
matplotlib>=3.5
seaborn>=0.12

# Data processing
dask>=2022.12
swifter>=1.3
cloudpickle>=2.0

# Deep learning (installed separately, listed for reference)
# torch>=2.0
# torchvision>=0.15

# Utilities
PyYAML>=6.0
tqdm>=4.60
psutil>=5.8
six>=1.15
networkx>=2.6
Pillow>=9.0
